/**
 * 
 * 22.括号生成
 * 
 * 问题
 * 虽然我看懂了题解，但，是我脑袋感觉 没有转过来，就是印象很浅，没有思考，感觉递归和回溯有点区别啊， 思考的方式不同？我递归都是在想这个递归函数的职责是啥，而回溯我反而想不明白了
 * 
 * 
 * 
 * 
 * 
 * 你的感觉完全抓到了痛点：**普通递归和回溯，大脑思考时的视角确实截然相反。**

你平时习惯的递归是“自顶向下拆解（分治）”**视角，而回溯是**“自底向上遍历决策树（走迷宫）”视角。视角没切换过来，就会觉得回溯代码怎么看怎么别扭。

---

**两种思维方式的本质区别**

* **普通递归（分治视角）：看重“返回值与职能”**
* **大脑思维**：“我不需要知道子问题怎么算出来的，我只要定义好函数 $f(n)$ 的意义，相信 $f(n-1)$ 能给我正确答案，然后拿来组合。”
* **典型例子**：归并排序、二叉树最大深度。函数通常有明确的返回值（例如返回一个数字、一个合并后的链表）。
* **数学对应**：数学归纳法、递推公式。


* **回溯（决策树遍历视角）：看重“全局路径与状态转移”**
* **大脑思维**：“我手头有一个全局的暂存器（比如一个 `StringBuilder`），我现在站在迷宫的岔路口，**下一步我能做哪几种选择？**选完走进去，探索完之后**必须原路退回来（恢复现场）**，换另一条路继续试。”
* **典型特征**：函数通常**没有返回值（`void`）**，靠操作参数里的集合（如 `res.add()`）收集结果。
* **核心关注点**：不是“这个函数能给我返回什么”，而是“当前节点的可用选项有哪些，走完怎么撤销”。

---


**用“回溯三步法”理顺思维**

遇到回溯题，不要再去套“函数定义是什么、子问题返回值是什么”，直接问自己这 3 个问题：

**1. 路径（当前走到哪了）：**

* 在这道题里，当前拼出的字符串是什么？已经用了几个 `(`，几个 `)`？（对应参数 `cur`, `open`, `close`）

**2. 选择列表与约束（当前步能干什么）：**

* 在当前位置，我只有两个动作可选：填 `(` 或填 `)`。
* 什么时候能填 `(`？左括号还没用完（`open < n`）。
* 什么时候能填 `)`？左括号比右括号多（`close < open`）。

**3. 撤销选择（恢复现场）：**

* 试完填 `(` 这条路后，退回当前路口：`cur.deleteCharAt(...)`，这样才能干干净净地去尝试填 `)`。

---

 */

class Solution {

    StringBuilder path = new StringBuilder();
    List<String> ret = new ArrayList<>();
    int left,right;
    public List<String> generateParenthesis(int n) {
        //left,right代表左括号的数量和右括号的数量
        left = 0;
        right = 0;
        work(n);
        return ret;
    }
    public void work(int n){
        if(path.length() == 2*n){
            ret.add(path.toString());
        }
        if(left < n){
            path.append('(');
            left++;
            work(n);
            left--;
            path.deleteCharAt(path.length() -1);
        }
        if(right < left){
            path.append(')');
            right++;
            work(n);
            right--;
            path.deleteCharAt(path.length() -1);
        }
    }
}